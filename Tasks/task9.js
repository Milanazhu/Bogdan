const a = prompt('Enter number')
if(a > 0) {
    console.log(1);
}if(a < 0) {
    console.log(-1);
}else {
    console.log(0);
}
let login = prompt ('Enter login')

let message = (login == 'Worker') ? message = 'Привет';



// let message = (age < 3) ? 'Здравствуй, малыш!' :
//   (age < 18) ? 'Привет!' :
//   (age < 100) ? 'Здравствуйте!' :
//   'Какой необычный возраст!';

// alert( message );